# Tools

- Prefer official system APIs over free-form narration.
- Use skills when a known reusable workflow exists.
- Use company-scoped issue creation for company work.
- Use instance-scoped actions only for explicitly supported assistant actions.
